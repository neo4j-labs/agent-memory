# TypeScript documentation implementation evidence

Scope: W05–W08, TS01–TS10, EX01/EX03/EX06. Current-source work only; no SDK behavior/features, package versions, tags, npm publication, deployed NAMS resources, production credentials, or generated TypeDoc HTML were changed. Runtime validation used Node 24.11.1 / npm 11.6.2 and the repository lockfiles. The documented source runtime floor is Node >=22; this local run did not independently execute Node 22. The CI matrix retains Node 22 and 24.

## Per-finding disposition

| ID | Implementation and evidence | Remaining boundary |
|---|---|---|
| TS01 | Replaced unsupported hosted tutorial operations with actual REST routes. Four complete runnable counterparts live in `typescript/examples/vercel-ai/src/tutorials/{first-agent,conversation,hosted-quickstart,knowledge-graph}.ts`, reproduced exactly in the four authored lessons. First-agent creates a conversation, stores/searches a scoped message, adds/reads an entity, wraps a V4 model and records/reads a hosted reasoning step. Graph lesson ingests two synthetic documents, checks storage, then boundedly polls a run-specific entity name and inspects returned workspace graph data. Mastra and Vercel/LangChain examples now avoid hosted preference/fact calls. | Extraction names/edges/traversals and deployed outcomes remain live-verification questions. No relationships/facts/legacy traces were invented to preserve an impossible lesson. |
| TS02 | Conversation lesson has separate teach, recall and cleanup modes. A second process receives only the retained conversation id and a new question. Real REST transport + local HTTP stub + a mock model prove the second process receives the first process's sentinel, while its new prompt does not restate the sentinel. Vercel example resume runs use different questions; Mastra explicitly selects prior-thread messages. | `userId`/`resourceId` alone does not resume, authorize, or retrieve all prior conversations. |
| TS03 | Removed V1/direct `experimental_middleware` patterns from lessons. Complete lessons compile using `wrapLanguageModel`, AI SDK 7 and V4 middleware without mismatch-hiding casts. | Registry artifacts and deployed framework versions are separate from current-source compilation. |
| TS04 | Rewrote error reference/how-to and corrected observability guarantees: 401/403 authentication; other ordinary REST 400/404/429/500 transport errors; optional correlation id; raw timeouts/errors; no automatic retry/RateLimitError/retryAfter/details. Added a bounded retry for an idempotent conversation read only. New `typescript/test/integration/docs-contract.test.ts` exercises all these statuses, single-attempt behavior, raw timeout, bulk overflow, missing search scope, unused namespace and unsupported methods. | No SDK normalization or retry semantics changed. |
| TS05 | MCP lesson now has a full ESM package setup, NodeNext tsconfig, exact src/server.ts → dist/server.js path, correct import placement and custom-tool insertion. `typescript/examples/mcp/test/docs-assembly.test.ts` extracts the authored server/config/custom block, compiles standalone temp projects, launches the emitted server via stdio and checks 12/13 tools. It runs in the example job after SDK build, not core tests before dist exists. | Claude UI/platform conventions are presented as optional/conventional; no fresh client UI claim or live server write is asserted. |
| TS06 | Corrected core middleware function-id resolution and streaming lifetime docs, edge guide, Next.js guide/README and Worker README. Core stream persistence exposes no completion promise. Vercel CLI now disables automatic assistant persistence and awaits its own successful assembled-response write; tests check no duplication. Worker guidance points to its existing explicit end/abort/error lifecycle. | Next.js minimal route remains background persistence and is explicitly limited. No SDK flush/retry API was added. Runtime termination cannot be guaranteed away. |
| TS07 | API map now covers all 65 async public subclient methods (14 short-term, 19 long-term, 13 reasoning, 1 query, 6 auth, 12 ontology), six accessors and lifecycle/options. Corrected update fields, trace id/filter/defaults, list limitations, short-term aliases, bulk cap, bridge-only capability rows, wait options and graph expansion. `namespace` documented as unused/no isolation. JSDoc-only changes correct namespace, wait errors and inherited Strands text; `@inline` renders referenced shapes without adding API exports. | Generated reference update belongs to existing automation/publication handoff. |
| TS08 | Node >=22 now consistent in selected SDK docs/tutorials/CONTRIBUTING; eve retains Node >=24. Removed fictional 0.5/0.6 dev-version verification footers and replaced them with explicit current-source/manifest/lockfile boundaries. | Local execution was Node 24; live and published-artifact validation were not performed by this lane. |
| TS09 | Provider README now accurately states both core middleware and separate provider use V4, explains feature/policy differences, uses `nams_` placeholders, uses npm pack's printed filename rather than stale 0.1.0 tarball, and distinguishes source provider from selected npm artifacts. Source setup builds core first, then installs it without saving over provider declarations. Provider examples index includes all six actual files (five scripts plus a route template), source dependencies and Labs caveats. | No provider package publication, live retrieval, or package code behavior was changed. |
| TS10 | MCP lesson verification stores messages/entities and records steps using returned ids; new chat includes the exact NAMS conversation id. Removed fictional separate preference records/dashboard Preferences outcome. Custom policy applies separately to custom tools. | Live MCP client UI and extraction results require separate verification. |
| EX01 | All nine example READMEs now require repository-root SDK install/build before entering/installing the local example. Examples index explains file dependencies, shared tsconfig, lockfile policy, standalone-copy artifact gates and Strands copied-package behavior. Existing `make ts-test-examples` passed all nine installs/typechecks/tests. A separate pristine temp tree started with no dist, built SDK offline from cached locked packages, installed Strands with install-links=true and passed lint against copied built exports. | `make ts-test-examples` runs typechecks/tests, not each app's production build; the separate CI job retains optional app build steps. No live apps launched. |
| EX03 | TypeScript CONTRIBUTING release checklist now includes package.json, src/version.ts, applicable locks, changelog, check:version, lint/tests/build/pack review before an authorized tag. Corrected TS lint/no ESLint, ESM-only build, Node22 and opt-in bridge TCK description. A temp aligned version passed the existing sync script; manifest-only mismatch failed with exact diagnostic. | No actual bump/tag/publish. Root owns Python/docs contributor sections. |
| EX06 | Fixed five-integration wording; removed LangChain drop-in/Document-instance/id-dedup claims and stale Strands spool path; corrected source/copy guidance. Broader executable sweep found unsupported REST preference/fact usage in Vercel, Mastra, LangChain and eve. Repaired first three using message/context/entity APIs and tightened their fakes/tests. Eve README prominently identifies unsupported profile/order paths and marks illustrative mocked output/deployment boundary. | Eve product/profile/checkout design is deliberately not rewritten. Its 46 passing fake tests do not prove hosted support. Root owns the root README/index integration. |

## Validation completed

- `npm run build` for core SDK passed.
- `npm run lint` passed for source and core test/conformance types.
- Core `npm test`: **34 files, 367 tests passed**, including 10 new REST contract cases. Log: `logs/agent-memory-core-tests.log`.
- `make ts-test-examples`: all nine example dependency installations and typechecks passed; **145 tests passed**: Worker 19, eve 46, LangChain 5, Mastra 11, MCP 10, Next.js 14, ontology 10, Strands 16, Vercel 14. Log: `logs/agent-memory-all-examples.log`.
- Vercel's 14 include seven complete lesson checks: four exact doc/program comparisons, hosted storage/search/entity/model/step path, two-process recall, bounded extraction success/timeout. No external model/network service used; a local HTTP stub rejects unhandled routes.
- MCP's new assembly test checks the documented server and optional custom tool against actual installed MCP types and stdio `tools/list`. Existing registration/dispatch suite remains in place.
- Clean no-dist source bootstrap and Strands copied-package check: passed. Log: `logs/agent-memory-clean-bootstrap.log`; temporary source tree removed after test.
- Temp release version sync: matching version exit 0; mismatched manifest/source exit 1 with `src/version.ts (9.9.8-docs-check) does not match package.json (9.9.9-docs-check).` Repo versions unchanged.
- `npm run docs:api -- --out /tmp/agent-memory-typedoc-implementation`: **144 HTML files, 0 warnings, 0 errors**. Inspected rendered namespace and waitForExtraction options/error comments. Log: `logs/agent-memory-typedoc-implementation.log`. No tracked generated output changed.
- `git diff --check` passed for lane files. Root's source docs validator found no TS-lane errors; its seven then-active comma-alt errors were on other lanes' pages.
- Added the four selected documentation path patterns to both push/PR triggers in `.github/workflows/ci-typescript.yml`, per root instruction. Existing build-before-example order remains unchanged.

## Failure evidence and handling

- Initial npm install failed because of sandbox cache/network access; writable cache alone was insufficient. Authorized escalation installed locked dependencies successfully. Initial log was overwritten by final install outcome; no dependency manifest/lockfile changes resulted.
- New loopback tests initially failed with sandbox `listen EPERM`; full output retained at `logs/agent-memory-vercel-tests-sandbox-failure.log`. Added explicit listen-error rejection, then authorized local-loopback execution passed.
- Initial contract assertion expected ValidationError for unscoped search; source and execution showed pre-request TransportError from path substitution. Corrected the test and documented the actual distinction. Full initial output retained at `logs/agent-memory-contract-tests-initial-failure.log`.

## Owned changed surfaces

- Five TypeScript tutorial pages, every `how-to/typescript/*.adoc` page, `reference/typescript-api.adoc`, `sdks/typescript.adoc`.
- `typescript/README.md`, `typescript/examples/README.md`, all nine example READMEs, provider package README and provider examples README.
- Vercel/Mastra/LangChain example programs/glue and their existing offline fixtures/tests; four new Vercel lesson programs and lesson test fixtures; new MCP assembly test; new core REST docs-contract test.
- JSDoc-only changes in `typescript/src/{types.ts,errors.ts,long-term/index.ts,integrations/langchain.ts,integrations/strands/memory-store.ts}`.
- Only TS publishing/runtime/lint/build/TCK sections of CONTRIBUTING, plus requested TS workflow docs path triggers.

No code runtime features or new backend routes were added. Runtime defects that need product changes remain explicit rather than disguised by a successful fake.

## W11 follow-up: historical section anchors

Restored all 54 meaningful anchors requested from the original rendered IDs across `how-to/typescript/authenticate.adoc`, `how-to/typescript/handle-errors.adoc`, and all five `tutorials/*-typescript.adoc` pages. Anchors route to corresponding setup, complete program, inspection, cleanup, or reference guidance; displaced unsupported preference/schema/relationship material was not restored. Added one brief authentication-reference pointer for discovery endpoints. Preserved blank lines between anchor paragraphs and heading/source blocks so headings and code remain rendered as blocks.

Validation: `node /tmp/check-doc-anchors.cjs` now reports **no lost IDs in any TypeScript how-to or TypeScript tutorial**. A separate Asciidoctor conversion found the expected headings and TypeScript code blocks on all seven pages and no literal `==` headings. Seven-page `git diff --check` passes. These final changes are anchor/prose-only; executable lesson blocks are unchanged.

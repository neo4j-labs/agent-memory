# Tutorial implementation evidence

Scope: seven assigned Python tutorials, nine new complete programs in the Antora `examples` resource family, one focused contract test module, and the explicitly authorized `examples/ontology-lifecycle` example. No live service writes, provider calls, model downloads, Docker startup, or deployment performed.

## Per-finding changes

- **ED01: one teachable path and explicit outcomes.** `tutorials/nams-quickstart.adoc:12` chooses a current-checkout production NAMS workspace-key path and checks actual returned conversation/trace IDs. `tutorials/anthropic-and-local-embeddings.adoc:12` chooses Anthropic extraction plus a local 384-dimensional embedder; its outcome is memory extraction/retrieval, not a nonexistent complete LLM chat agent. `tutorials/strands-agent-quickstart.adoc:74` uses two processes and checks a restored sentinel before the second model request. `tutorials/microsoft-agent-memory.adoc:12` selects the current OpenAI Responses client and an explicitly fictional catalog. `tutorials/mcp-server.adoc:14` selects macOS Claude Desktop, local stdio, and the core profile, then verifies one exact persisted record separately from conversational recall. Prerequisites, assembly, checks, cleanup and onward task links exist on all seven.
- **ED02: explicit assembly and runnable maintained source.** Every lesson includes complete files from `docs/modules/ROOT/examples/`; commands execute those exact files in the cloned checkout. API return values are propagated; no hand-copied placeholder IDs. All imports and entrypoints are included. Current source availability is distinguished from wheel release and live-service verification. The Anthropic, NAMS and Microsoft readbacks use the actual public `get_conversation(...).messages` contract.
- **ED03: hosted lifecycle failure behavior.** `tutorials/skills-quickstart.adoc:64` records the service's run ID, polls queued and running states to a finite deadline, requires a skill ID for Created, and stops on Withheld/Failed, malformed output or HTTP error. Review, approve/publish and download are separate reader-invoked steps. `tutorials/skills-quickstart.adoc:101` verifies a ZIP contains SKILL.md, saves attestation separately, and does not claim loading/executing the skill. The seed is one repeated, explicitly simulated three-step refund-decision procedure, rather than unrelated tasks.
- **ED04: verifiable onboarding and recovery.** `tutorials/ontology-quickstart.adoc:23` captures the prior active version before cloning; missing metadata stops before mutation. The shared helper restores and verifies that version in a finally block before deleting the clone. Restoration failure retains the clone and propagates with inspection IDs. No delete-means-restore claim remains. Every tutorial names its observable success and failure states rather than promising exact model-generated text or entity counts.
- **PY17: ontology domain assertions and migrations.** `docs/modules/ROOT/examples/ontology_quickstart.py` reads the actual template and tests an intentionally nonexistent entity type, avoiding invented Patient property requirements. `examples/ontology-lifecycle/main.py:189` now raises on timeout, failed migration or errored nodes so subsequent operations do not continue; extraction timeout also stops before migration. Its docstring and README describe disposable-workspace use, retained activation and irreversible data migration rather than “re-running is safe”/delete-to-restore. README sample output is explicitly illustrative, not live evidence.

## Files and preservation

Tutorial paths retain all 90 old generated section IDs: nams 15, ontology 8, skills 8, MCP 15, Strands 16, Microsoft 16, Anthropic 12. Titles and primary reading path were rewritten in sentence case; historical anchors remain at relevant replacement sections. No tutorial image assets are active in these seven pages. Local xref targets all exist; no new guessed API routes or pages.

Complete source files: `hosted_tutorial_helpers.py`, `nams_quickstart.py`, `ontology_quickstart.py`, `skills_quickstart.py`, `wait_for_tutorial_neo4j.py`, `anthropic_local_memory.py`, `strands_memory_tutorial.py`, `microsoft_shopping_tutorial.py`, `mcp_local_tutorial.py`.

## Verification

- `.venv/bin/python -m pytest tests/docs/test_hosted_tutorial_contracts.py tests/examples/test_ontology_lifecycle_example.py -q`: **48 passed**. One preexisting google-cloud-storage deprecation warning from the shared test import environment.
- Focused contracts cover bounded queued/running polling, withheld/failed/malformed outcomes, restoration ordering, missing prior version, failure retaining clone, propagation of server-created IDs, seed consistency, versioned REST routes, explicit publication boundary, HTTP failures, ZIP content, compilation of complete programs, migration failure/timeout stopping the following operation, and readback method availability on actual Bolt/NAMS classes. Fake backends are offline evidence, not proof of live model/service behavior.
- Ruff format/check passed for all nine fixture programs, new tests and the ontology lifecycle main program.
- Local Asciidoctor conversion with expanded maintained includes preserved all 90 old anchors, found no duplicate IDs or missing local xref files, and emitted zero diagnostics. Evidence: `logs/tutorials-render-validation.json`, `/tmp/tutorials-rendered/`. Full fresh Antora/link checks remain root's final gate.

## Remaining limits

No live NAMS skill quality-gate outcome, signed attestation, template availability, Bedrock inference-profile access, provider model availability, Claude Desktop interaction or real database run was claimed. Lessons state service prerequisites and fail visibly when these are unmet. The full ontology lifecycle example intentionally migrates data and retains its last active version; this differs explicitly from the smaller restoring tutorial. Product-source compatibility fixes, published-wheel validation and full-site publication are outside this batch.

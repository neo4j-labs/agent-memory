= ShortTermMemory API reference
:page-product: agent-memory
:page-pagination:
:toclevels: 2

Conversation and message operations exposed through `client.short_term`. Signatures in the bolt sections describe `ShortTermMemory`; the NAMS table identifies hosted differences.

Signatures below show types, keyword-only arguments (`*`), and defaults. They are reference declarations; the examples show calls to execute.

See xref:reference/backend-capabilities.adoc[Backend capabilities] for availability and data scope. This page is a short overview and index; each subsystem has its own page.

== Overview

[source,python]
----
message = await client.short_term.add_message(
    session_id="example", role="user", content="Hello, world!"
)
conversation = await client.short_term.get_conversation("example")
for stored in conversation.messages:
    print(stored.role.value, stored.content)
----

The example uses bolt, where a session name can create a conversation implicitly. On NAMS, call `create_conversation` first and use its returned ID.

== On this subsystem

* anchor:_message_operations_bolt[]anchor:_add_message[]anchor:_add_messages_batch[]anchor:_get_conversation[]anchor:_delete_message[]anchor:_nams_and_shared_conversation_operations[]anchor:_create_conversation[]anchor:_list_conversations[]anchor:_bulk_add_messages[]anchor:_get_observations[]anchor:_get_reflections[]anchor:_get_extraction_status[]anchor:_models[]anchor:_message[]anchor:_conversation[]anchor:_extractionstatus[]anchor:_message_operations[]anchor:_parameters[]anchor:_returns[]anchor:_add_messages[]anchor:_get_messages[]anchor:_get_message[]anchor:_extraction_status_nams[]anchor:_message_model[]xref:reference/api/short-term-messages.adoc[Conversation and message operations] — `add_message`, `add_messages_batch`, `get_conversation`, `delete_message`, the NAMS conversation operations (`create_conversation`, `list_conversations`, `bulk_add_messages`, `get_observations`, `get_reflections`, `get_extraction_status`), and the `Message`/`Conversation`/`ExtractionStatus` models.
* anchor:_search_operations_bolt[]anchor:_search_messages[]anchor:_get_context[]anchor:_search_operations[]anchor:_parameters_2[]anchor:_returns_2[]xref:reference/api/short-term-search.adoc[Message search and context] — `search_messages` and `get_context`.
* anchor:_session_operations_bolt[]anchor:_list_sessions[]anchor:_clear_session[]anchor:_conversation_summaries_bolt[]anchor:_get_conversation_summary[]anchor:_message_linking_and_deferred_processing_bolt[]anchor:_migrate_message_links[]anchor:_generate_embeddings_batch[]anchor:_extract_entities_from_session[]anchor:_sessioninfo[]anchor:_conversationsummary[]anchor:_session_operations[]anchor:_returns_3[]anchor:_delete_session[]anchor:_conversation_summaries[]anchor:_returns_4[]anchor:_generate_summary[]anchor:_get_summary[]anchor:_message_linking[]xref:reference/api/short-term-sessions.adoc[Sessions, summaries, and maintenance] — `list_sessions`, `clear_session`, `get_conversation_summary`, `migrate_message_links`, `generate_embeddings_batch`, `extract_entities_from_session`, and the `SessionInfo`/`ConversationSummary` models.

anchor:_example[]

== See also

* xref:reference/api/memory-client.adoc[MemoryClient]
* xref:how-to/messages.adoc[Store and search messages]
* xref:reference/nams-limits.adoc[NAMS behavior]

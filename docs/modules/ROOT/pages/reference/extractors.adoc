= Extractor classes reference
:page-product: agent-memory
:page-pagination:
:toclevels: 2

Local Python extraction classes and result models. They can be used independently or injected into a bolt MemoryClient; they do not configure the hosted NAMS pipeline.

Signatures below show types, keyword-only arguments (`*`), and defaults. They are reference declarations; the examples show calls to execute.

See xref:reference/backend-capabilities.adoc[Backend capabilities] for availability and data scope. This page is a short overview and index; each subsystem has its own page.

== On this subsystem

* anchor:_constructor[]anchor:_parameters[]anchor:_default_type_mapping[]anchor:_example[]anchor:_spacyentityextractor[]anchor:_constructor_2[]anchor:_parameters_2[]anchor:_factory_methods[]anchor:_example_2[]anchor:_glinerentityextractor[]anchor:_extract[]anchor:_extract_batch[]anchor:_for_schema[]anchor:_for_poleo[]anchor:_from_config[]anchor:_update_labels[]anchor:_add_label_mapping[]anchor:_constructor_3[]anchor:_parameters_3[]anchor:_example_3[]anchor:_llmentityextractor[]anchor:_for_custom_types[]xref:reference/extractor-classes.adoc[Built-in extractor classes] — `SpacyEntityExtractor`, `GLiNEREntityExtractor`, and `LLMEntityExtractor`.
* anchor:_glirelextractor[]anchor:_extract_relations[]anchor:_glinerwithrelationsextractor[]xref:reference/extractor-relations.adoc[Relation extractors] — `GLiRELExtractor` and `GLiNERWithRelationsExtractor`.
* anchor:_merge_strategies[]anchor:_constructor_4[]anchor:_example_4[]anchor:_extractionpipeline[]anchor:_extract_with_details[]anchor:_add_stage[]anchor:_remove_stage[]anchor:_merge_by_confidence[]anchor:_merge_by_union[]anchor:_merge_by_intersection[]anchor:_merge_by_cascade[]anchor:_pipelineresult[]anchor:_stageresult[]anchor:_batchextractionresult[]anchor:_batchitemresult[]xref:reference/extractor-pipeline.adoc[Extraction pipeline] — `ExtractionPipeline`, merge strategies, and the pipeline/batch result models.
* anchor:_builder_methods[]anchor:_constructor_5[]anchor:_example_5[]anchor:_extractorbuilder[]anchor:_with_spacy[]anchor:_with_gliner[]anchor:_with_gliner_schema[]anchor:_with_llm[]anchor:_with_llm_fallback[]anchor:_with_schema[]anchor:_with_entity_types[]anchor:_with_confidence_threshold[]anchor:_build[]anchor:_extract_preferences[]xref:reference/extractor-builder.adoc[Extractor builder] — `ExtractorBuilder` fluent construction.
* anchor:_streamingextractor[]anchor:_extract_streaming[]anchor:_chunk_document[]anchor:_extract_to_result[]xref:reference/extractor-streaming.adoc[Streaming extraction] — `StreamingExtractor` for chunked long-document extraction.

== EntityExtractor protocol

The protocol requires `extract`, not `extract_batch`. Batch methods are concrete capabilities with different signatures on GLiNER and ExtractionPipeline; check the selected class.

// contract: extraction/base.py:EntityExtractor.extract
[source,python]
----
async def extract(
    text: str,
    *,
    entity_types: list[str] | None=None,
    extract_relations: bool=True,
    extract_preferences: bool=True,
) -> ExtractionResult: ...
----

== Result models

These are Pydantic models imported from `neo4j_agent_memory.extraction`. `ExtractedEntity` uses `type` and `attributes`, not `entity_type` and `metadata`. ExtractionResult has entities, relations, preferences, and source_text; it has no metadata envelope.

=== ExtractionResult

Fields and defaults:

[cols="1,2,1,3",options="header"]
|===
| Field | Type | Default | Description
| `entities` | `list[ExtractedEntity]` | `[]` |
| `relations` | `list[ExtractedRelation]` | `[]` |
| `preferences` | `list[ExtractedPreference]` | `[]` |
| `source_text` | `str \| None` | `None` | Original source text
|===

=== ExtractedEntity

Fields and defaults:

[cols="1,2,1,3",options="header"]
|===
| Field | Type | Default | Description
| `name` | `str` | `required` | Entity name
| `type` | `str` | `required` | Entity type (PERSON, OBJECT, LOCATION, EVENT, ORGANIZATION)
| `subtype` | `str \| None` | `None` | Entity subtype (e.g., VEHICLE for OBJECT, ADDRESS for LOCATION)
| `start_pos` | `int \| None` | `None` | Start position in text
| `end_pos` | `int \| None` | `None` | End position in text
| `confidence` | `float` | `1.0` | Confidence score
| `context` | `str \| None` | `None` | Surrounding context
| `attributes` | `dict[str, Any]` | `{}` | Additional attributes extracted for this entity
| `extractor` | `str \| None` | `None` | Name of the extractor that produced this entity
|===

=== ExtractedRelation

Fields and defaults:

[cols="1,2,1,3",options="header"]
|===
| Field | Type | Default | Description
| `source` | `str` | `required` | Source entity name
| `target` | `str` | `required` | Target entity name
| `relation_type` | `str` | `required` | Type of relationship
| `confidence` | `float` | `1.0` | Confidence score
|===

=== ExtractedPreference

Fields and defaults:

[cols="1,2,1,3",options="header"]
|===
| Field | Type | Default | Description
| `category` | `str` | `required` | Preference category
| `preference` | `str` | `required` | The preference statement
| `context` | `str \| None` | `None` | Context where preference applies
| `confidence` | `float` | `1.0` | Confidence score
|===

`ExtractionResult.entity_count`, `relation_count`, and `preference_count` are properties. `entities_by_type()` groups entities into a dictionary; `get_entities_of_type(type)` returns a list using case-insensitive type matching. `filter_invalid_entities()` returns a new result with invalid names and affected relations removed. There are no `filter_by_type` or `filter_by_confidence` methods.

[source,python]
----
people = result.get_entities_of_type("PERSON")
high_confidence = [entity for entity in result.entities if entity.confidence >= 0.7]
print(result.entities_by_type())
----

`ExtractedEntity.normalized_name` lowercases/normalizes its name and `full_type` includes its subtype. `ExtractedRelation.as_triple` returns source, relation type, and target.

== See also

* xref:reference/schemas.adoc[Domain schemas]
* xref:how-to/entity-extraction.adoc[Configure entity extraction]
* xref:how-to/batch-processing.adoc[Batch processing]
* xref:how-to/entity-extraction-batch.adoc[Batch & Streaming] — handling `BatchExtractionResult` and `StreamingChunkResult` errors

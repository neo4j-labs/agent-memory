= The POLE+O data model
:page-product: agent-memory
:page-pagination:
:toclevels: 3
:sectanchors:
:icons: font
:source-highlighter: highlight.js

POLE+O is the default entity classification in Neo4j Agent Memory. Its broad categories provide a common vocabulary for extracted knowledge while leaving room for domain-specific detail.

NOTE: The shared categories do not make configuration identical across backends. Bolt uses client-side schema configuration; NAMS uses versioned server-side ontologies. See xref:reference/backend-capabilities.adoc[Backend capabilities].

image::diagrams/poleo-model.png["POLE+O entity model: Person, Object, Location, Event, and Organization categories with example subtypes",width=600,align=left,link=self]

[[_what_is_poleo]]
== What is POLE+O?

POLE+O stands for *Person, Object, Location, Event, and Organization*. It separates who or what a statement concerns from the statement itself.

For a fictional example, Maya Chen (`PERSON`) attends a robotics workshop (`EVENT`) at a Denver laboratory (`LOCATION`) hosted by Northstar Robotics (`ORGANIZATION`), bringing a sensor prototype (`OBJECT`). The categories make these records comparable without forcing every application to use the same domain vocabulary.

[[_entity_types_subtypes]]
== Entity types and subtypes

anchor:_person[]anchor:_object[]anchor:_location[]anchor:_event[]anchor:_organization[]

* `PERSON` describes people or identified personas. A name or role alone may still be ambiguous.
* `OBJECT` covers physical and digital items, such as a device or document.
* `LOCATION` covers places and geographic areas. Coordinates are additional data, not a prerequisite for identifying a place.
* `EVENT` describes something that happens, such as a meeting or transaction.
* `ORGANIZATION` describes companies, institutions, and groups.

A subtype refines the broad category. For example, `OBJECT` with subtype `VEHICLE` retains its object classification while allowing more specific queries. A subtype is classification, not proof that two entities are identical.

The xref:reference/schemas.adoc[schema reference] owns the complete supported type and subtype lists. A domain may need distinctions beyond these defaults; that is a reason to model the domain explicitly, not to overload a name field.

[[_neo4j_schema]]
== Classification in the graph

anchor:_label_structure[]anchor:_custom_entity_types[]anchor:_relationships[]anchor:_message_links[]

On Bolt, a managed entity carries the `:Entity` label and a `type` property, with valid type and subtype labels added for querying. For example, an `OBJECT` with subtype `VEHICLE` can carry `:Entity:Object:Vehicle`.

The labels describe the node; relationships describe how it connects to other nodes. Message extraction links `:Message` to `:Entity` with `:MENTIONS`. Extracted relationships can be represented as `:RELATED_TO` with a property identifying the relation. Do not assume that a conceptual employment relation is always stored as a physical `:WORKS_AT` edge.

See xref:reference/schema-objects.adoc[Schema objects] and xref:reference/api/long-term.adoc[Long-term memory API] for the stored shapes and operations. Existing domain graphs can retain their own labels and relationships through xref:how-to/adopt-existing-graph.adoc[graph adoption].

[[_configuring_poleo]]
== Choosing the classification scope

anchor:_using_default_poleo[]anchor:_selecting_specific_poleo_types[]anchor:_using_subtypes[]

A narrow extraction vocabulary reduces irrelevant candidates, while a broader vocabulary can preserve unexpected entities. The appropriate scope depends on what the application needs to retrieve later.

Keep configuration separate from this conceptual choice. Follow xref:how-to/entity-extraction.adoc[Configure extraction] for the Bolt procedure and xref:how-to/use-ontologies.adoc[Use ontologies] for NAMS. The xref:reference/configuration.adoc[configuration reference] defines the actual settings and defaults.

[[_custom_schema_models]]
== When the defaults are insufficient

anchor:_schemamodel_options[]anchor:_using_custom_entity_types[]anchor:_loading_schema_from_file[]

A domain schema may need types such as a research study or a device fault, with properties and relationships that the five categories do not fully describe. A custom model makes those distinctions explicit.

On Bolt, `SchemaModel.CUSTOM` and schema configuration describe the client-side model. On NAMS, an ontology refines the common categories through versioned definitions and validation modes. These mechanisms have different ownership and validation behavior; configuring one does not activate the other.

See xref:explanation/ontologies.adoc[Ontologies], xref:reference/configuration.adoc[Schema configuration], and xref:how-to/entity-extraction-schemas.adoc[Use extraction schemas].

[[_working_with_entities]]
== Classification and identity are different

anchor:_searching_entities[]anchor:_entity_resolution[]

Type filters help retrieve the right class of entity. Identity resolution asks whether two records denote the same individual thing. Both are needed: two `PERSON` records named Maya Chen are not necessarily duplicates.

Stable identifiers and provenance can resolve ambiguity that names and types cannot. xref:explanation/resolution-deduplication.adoc[Resolution and deduplication] explains those decisions; xref:how-to/entities.adoc[Work with entities] provides the storage and retrieval procedure.

[[_poleo_in_the_extraction_pipeline]]
== How extraction reaches the common vocabulary

anchor:_spacy_mapping[]anchor:_gliner_labels[]anchor:_llm_prompt[]

Extractors start with different representations. A spaCy model has trained labels, GLiNER accepts configurable labels and descriptions, and an LLM uses a response schema and prompts. Mappings translate the relevant output into the entity vocabulary.

A mapping is not semantic validation. A model can assign the wrong label, and a broad fallback category can hide a missing domain distinction. Inspect representative output when changing schemas. See xref:reference/extractors.adoc[Extractors], xref:reference/schemas.adoc[Domain schemas], and xref:explanation/extraction-pipeline.adoc[the extraction pipeline].

[[_best_practices]]
== Keeping a useful vocabulary

anchor:_choosing_entity_types[]anchor:_entity_naming[]anchor:_performance[]

Use consistent names and keep the source of an assertion. Add subtypes or custom types when they express a meaningful distinction needed by the application. Avoid treating product variants, subsidiaries, or same-name people as aliases solely to simplify the graph.

The model should follow retrieval needs: a distinction that matters for a query must survive ingestion. No general performance advantage follows merely from choosing a subtype over a custom type.

[[_next_steps]]
== Related documentation

* xref:reference/schemas.adoc[Domain schemas and subtype catalog]
* xref:reference/schema-objects.adoc[Graph schema objects]
* xref:how-to/entities.adoc[Store and retrieve entities]
* xref:explanation/ontologies.adoc[NAMS ontologies]

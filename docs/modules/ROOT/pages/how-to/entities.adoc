= Store entities, relationships, and facts
:page-product: agent-memory
:page-pagination:
:toclevels: 2
:sectanchors:

To build explicit graph knowledge, create typed entities, connect their returned IDs, and read the stored relationship back. Use POLE+O types with optional subtypes to distinguish domain concepts such as products.

anchor:_prerequisites[]

== 1. Prepare the Bolt client

Use a dedicated AuraDB instance from xref:tutorials/first-agent-memory.adoc#_step_2_set_up_neo4j[the first memory tutorial's Aura setup], with exported `NEO4J_URI`, `NEO4J_USERNAME`, and `NEO4J_PASSWORD`. Set `OPENAI_API_KEY` for the selected embedding provider. Prepare the example files below and install the provider extra:

include::partial$python-tutorial-setup.adoc[]

[source,bash]
----
python -m pip install 'neo4j-agent-memory[openai]==0.6.0'
----

The maintained program `core_memory_recipes.py` imports `core_memory_settings.py` from the same directory. That helper explicitly selects Bolt and disables automatic extraction/enrichment. Each command creates a new ID suffix so it can run independently. Embedding requests send example text to OpenAI.

These tasks use Python with Bolt. Consult xref:reference/backend-capabilities.adoc[backend capabilities] before adapting them to NAMS; preferences, facts, direct relationship writes, and this deduplication configuration are not portable hosted operations.

include::partial$python-core-recipe-files.adoc[]

== 2. Apply the task function

`add_entity` returns `(Entity, DeduplicationResult)` on Bolt. Repeated exact name/type writes merge at storage even with `deduplicate=False`. Python 0.6.0 returns the existing stored ID for SDK-created entities, so the returned entity can be used as a relationship endpoint. The recipe disables resolution and deduplication for its isolated example names, then verifies the stored edge. `add_fact` takes `obj` (or its third positional argument), and `Fact.as_triple` is a property.

The function below is included from the complete maintained program, which already supplies imports, client setup, unique IDs, and `asyncio.run`. Run the file in step 3; this extract is not a separate top-level script.

[source,python]
----
include::example$core_memory_recipes.py[tag=entities]
----

== 3. Run and verify

[source,bash]
----
python core_memory_recipes.py entities
----

Expected: `Verified: entity relationship and fact readback; person=...`. The recipe checks the exact source/target names and stored logical relationship type, then checks the fact triple returned by `get_facts_about`.

The command retains its example records for inspection. Remove the disposable tutorial database using that tutorial's cleanup command when finished. Do not substitute an unrestricted delete query against an existing database.

anchor:_search_entities[]

== Search or list entities

Use `search_entities(query, entity_types=["PERSON"], threshold=0.7, limit=10)` for semantic candidates. Similarity is in `entity.metadata["similarity"]`, not a top-level `score` field. An empty semantic query is not an all-entities listing.

For an exact ID/type/property lookup on Bolt, use `client.query.cypher` with parameters. Entity `attributes` and metadata may be serialized fields; inspect xref:reference/schema-objects.adoc[the stored schema] before writing a property filter. `get_entity_by_name` checks names/canonical names/aliases but has no type argument.

anchor:_create_relationships[]

== Preserve relationship and source meaning

Explicit `add_relationship` calls store the logical relationship name in `RELATED_TO.type`; automatic message extraction uses the separate `RELATED_TO.relation_type` property. Python 0.6.0 returns the stored relationship ID when an existing edge is matched and raises `NotFoundError` when an endpoint is absent. Its write still does not persist the `attributes` argument; do not depend on a provenance attribute surviving that call. Use `link_entity_to_message` for `EXTRACTED_FROM` links and `get_entity_provenance` for inspection. The xref:tutorials/knowledge-graph.adoc[document tutorial] demonstrates complete extraction and provenance storage.

anchor:_update_entities[]
anchor:_delete_entities[]

== Update an existing graph deliberately

There are no Python `update_entity`, `delete_entity`, `merge_entities`, or `get_entity` wrappers in these stores. For manual duplicate resolution use xref:how-to/deduplication.adoc[`merge_duplicate_entities`]. For another Bolt mutation, implement a parameterized application query through `client.graph.execute_write`, validate the exact affected IDs, and consider any linked provenance. `client.query.cypher` is read-only; hosted query access does not provide an equivalent write path.

For enrichment and geospatial tasks, follow xref:reference/configuration.adoc#enrichment[enrichment configuration] and xref:reference/api/long-term.adoc[geospatial operations]. Full parameters, result models, facts, and relationship queries are in xref:reference/api/long-term.adoc[the long-term reference].

// Historical section links resolve to the maintained task and reference pointers.
anchor:_typescript_equivalents_at_a_glance[]
anchor:_add_entities_manually[]
anchor:_basic_entity_creation[]
anchor:_entity_with_properties[]
anchor:_entity_with_description[]
anchor:_semantic_search[]
anchor:_filter_by_type[]
anchor:_filter_by_properties[]
anchor:_direct_relationships[]
anchor:_find_related_entities[]
anchor:_graph_traversal[]
anchor:_modify_properties[]
anchor:_merge_entities[]
anchor:_remove_single_entity[]
anchor:_bulk_delete[]
anchor:_work_with_facts[]
anchor:_add_facts[]
anchor:_query_facts[]
anchor:_entity_enrichment[]
anchor:_enable_enrichment[]
anchor:_manual_enrichment[]
anchor:_entity_statistics[]
anchor:_best_practices[]
anchor:_1_use_consistent_entity_types[]
anchor:_2_design_for_queries[]
anchor:_3_include_searchable_descriptions[]
anchor:_4_track_provenance[]
anchor:_see_also[]

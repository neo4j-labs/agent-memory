= Use with Microsoft Agent Framework
:page-product: agent-memory
:page-pagination:
:toclevels: 2
:sectanchors:

Give a Microsoft Agent Framework agent retrieved Neo4j context, persist its turns through the context-provider hook, and verify an explicitly recorded task trace. The selected example uses the current `Agent` API and `OpenAIChatClient`.

== 1. Prepare the selected environment

anchor:_overview[]
anchor:_installation[]

Use Python 3.10+ and a POSIX shell. The recipe uses `BoltSettings` to select Neo4j explicitly and disables entity extraction so that message persistence can be checked independently. Use a dedicated AuraDB instance with vector-index support and no incompatible existing vectors. Follow xref:tutorials/first-agent-memory.adoc#_step_2_set_up_neo4j[the Aura connection setup] and copy its connection values into the exports below. These recipes use the published Python 0.6.0 package; provider access and database execution must be verified in your environment.

include::partial$python-tutorial-setup.adoc[]

[source,bash]
----
python -m pip install 'neo4j-agent-memory[microsoft-agent,openai]==0.6.0' "agent-framework-openai>=1.13,<2"
export NEO4J_URI='neo4j+s://<instance-id>.databases.neo4j.io'
export NEO4J_USERNAME='neo4j'
export NEO4J_PASSWORD='replace-with-your-Aura-password'
export NEO4J_DATABASE='neo4j'
export OPENAI_API_KEY='replace-with-your-provider-key'
export OPENAI_MODEL='replace-with-an-available-model-id'
----

The OpenAI-backed recipes require access to `text-embedding-3-small`; it produces 1536-dimensional vectors in this configuration. Configure the selected chat model separately where required. Credentials are read from the environment, never printed by the programs.

The repository's `microsoft-agent` extra selects the current supported `agent-framework-core` 1.x range. The program imports `OpenAIChatClient` from `agent_framework.openai`; an OpenAI key is appropriate for that client. An Azure client would require its own deployment, endpoint and credentials.

== 2. Read the complete integration

Save both complete files below in `agent-memory-tutorials/`. The program imports `aura_config` from the helper in the same directory.

include::partial$python-aura-connection-file.adoc[]


.Complete `microsoft_shopping_tutorial.py`
[%collapsible]
====
.Save as `microsoft_shopping_tutorial.py`
[source,python]
----
include::example$microsoft_shopping_tutorial.py[]
----
====

The program seeds an explicit preference and places two fictional products in agent instructions. `Neo4jMicrosoftMemory.context_provider` is the sole message writer. A separate `record_agent_trace` call records the observed task outcome and its returned ID is read back.

== 3. Run and verify stored records

anchor:_quick_start[]
anchor:_using_neo4jcontextprovider[]
anchor:_using_neo4jchatmessagestore[]
anchor:_using_neo4jmicrosoftmemory_unified_interface[]
anchor:_tool_execution[]
anchor:_gds_integration[]
anchor:_gds_fallback_behavior[]
anchor:_reasoning_traces[]
anchor:_target_version[]
anchor:_version_matrix[]
anchor:_breaking_change_detection[]
anchor:_neo4jcontextprovider[]
anchor:_neo4jchatmessagestore[]
anchor:_neo4jmicrosoftmemory[]
anchor:_functions[]

[source,bash]
----
python microsoft_shopping_tutorial.py
----

Expected: a nonempty response, confirmation that the context-provider hook persisted the exact user turn, and a verified trace. If the model raises an exception, the program records a failed task outcome and propagates it. The program does not query live inventory or promise an automatically inferred preference. For a complete database-startup walkthrough, see xref:tutorials/microsoft-agent-memory.adoc[the Microsoft tutorial].

== Troubleshooting and cleanup

anchor:_troubleshooting[]
anchor:_importerror_agent_framework_not_found[]
anchor:_importerror_cannot_import_name_basecontextprovider[]
anchor:_gds_not_available_warning[]
anchor:_empty_context[]
anchor:_serialization_errors[]

Check that the installed framework exposes the current `Agent` and `ContextProvider` interfaces before adapting legacy code. An OpenAI/Azure credential mismatch is a provider configuration error, not a Neo4j failure. Empty retrieved context does not prove data is absent; inspect direct conversation readback and the configured retrieval thresholds. Client shutdown does not delete stored preferences, conversations or traces; retain the printed IDs in the dedicated example database until inspection is complete.

== Adapt the integration and inspect example applications

anchor:_pass_through_your_agent_framework_client[]
anchor:_memory_tools[]
anchor:_available_tools[]
anchor:_context_composition[]
anchor:_serialization[]
anchor:_complete_example[]
anchor:api-compatibility[]
anchor:_apis_used[]
anchor:_api_reference[]
anchor:_see_also[]

`Neo4jContextProvider` uses `before_run()` to supply context and `after_run()` to persist messages. `Neo4jChatMessageStore` supports explicit history storage; using it in addition to the writing context provider requires you to avoid duplicate writes. `Neo4jMicrosoftMemory` composes the two surfaces and exposes its `context_provider` for `Agent(context_providers=[...])`.

The context provider can include short-term history, long-term knowledge and similar recorded traces with configured limits. Session and user identifiers are personalization/grouping inputs, not a universal access boundary. Serialized provider state records configuration; it is not a database backup or a credential export.

.Integration components; graph analytics are optional
image::diagrams/microsoft-agent-architecture.png["Microsoft Agent Framework context hooks and optional memory tools connect to Neo4j Agent Memory",width=600,align=left,link=self]

The optional GDS helper requires suitable domain data and a supported graph surface. A Cypher fallback is a different retrieval/ranking method, not an equivalent execution of PageRank or Node Similarity. The minimal script above does not enable GDS. `create_memory_tools(...)` returns tool definitions that require the corresponding execution path; inspect the installed adapter before translating them into framework tool objects.

https://github.com/neo4j-labs/agent-memory/tree/main/examples/microsoft_agent_retail_assistant[The maintained retail application] adds inventory, cart and product-search tools plus a UI and domain graph. Those features belong to the example application, not to `MemoryClient` automatically.

.Separate TfL Explorer example application
image::diagrams/tfl-explorer-app-screenshot.png["TfL Explorer example interface showing a route-planning conversation",width=540,align=left,link=self]

The screenshot illustrates a separate application (capture date not recorded in the repository); it is not the output of the shopping recipe or evidence that the memory library itself provides live transit data.

See xref:reference/api/adapters.adoc[Provider adapter reference], xref:how-to/migrate-to-providers.adoc[Provider configuration], xref:reference/backend-capabilities.adoc[Backend capabilities] and xref:explanation/memory-types.adoc[Memory concepts]. Neo4j Agent Memory is a community-supported Neo4j Labs project.
